import styled from 'styled-components';

export const SignOut = styled.button`
  font-size: 2.5em;
  color: #d8e6f7;
  padding: 20px 0;
  transition: .4s;
  cursor: pointer;

  :hover {
    transition: .4s;
    transform: scale(1.1);
    color: var(--nav-text-color);
  }
`;
